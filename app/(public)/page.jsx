import Feed from '@components/Feed';

const Home = () => {
  return (
    <section className="w-full flex-center flex-col">
      <div>home page which is in the public group</div>

      {/* <Feed /> */}
    </section>
  );
};

export default Home;
